#include <stdio.h>
int main() {
    int n,m,l;
    scanf("%d %d %d",&n,&m,&l);
    if ((a+b)>c || (b+c)>c || (a+c)>b){
        printf("Valid");
    }
    else if
    print("Invalid");
    return 0;
}